<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <h1>Hello tout le monde</h1>
    <script>
      /*
        if (val > 0){
            console.log('la valeur est positive')
        }else if(val < 0){
            console.log('la valeur est négative')
        }else{
            console.log('la valeur est nulle')
        }*/

      /*        let val = prompt('Quel est votre age ?')
        val = Number(val)
        let letter = 'l'

        switch(val){
            case 0:
                if(letter === 'l'){
                    console.log('vous avez saisie une lettre l')
                }
                else{
                    console.log('la lettre est différente de l')
                }
                   console.log('vous avez saisie une valeur nulle')
                break;
            case 1:
                console.log('vous avez saisie une valeur 1')
                break;
            default:
                console.log('vous avez saisie une valeur inconnue')
                break;
        }

        let result;

        let numb1 = prompt('Quel est votre premier nombre ?')
        let numb2 = prompt('Quel est votre deuxieme nombre ?')

        let operation = prompt('Quelle est votre opération ?')

        numb1 = Number(numb1)
        numb2 = Number(numb2)

        switch(operation){
            case '+':
                result = numb1 + numb2

                if(result > 0){
                    console.log(`${numb1} + ${numb2} = ${result} est positif`)
                }
                else{
                    console.log(`${numb1} + ${numb2} = ${result} est négatif`)
                }
                break;
            case '-':
                result = numb1 - numb2
                if(result > 0){
                    console.log(`${numb1} + ${numb2} = ${result} est positif`)
                }
                else{
                    console.log(`${numb1} + ${numb2} = ${result} est négatif`)
                }
                break;
            default:
                console.log('l\'opération est différente de + ou -')
                break;
        }*/

      // Scope des variables

      // Variable globale
      /*    let age = 17 

        switch(age){
            case 17:
                // Variable locale
                const name = 'John'
                console.log(age)
                break;
            default:
                console.log('Fin')
        }  

        console.log(name)
        */
      //const age = Number(prompt('Quel est votre age : '));
      /*        let majeur = age >= 18 ? "Majeur" : "Mineur";
        console.log(majeur);*/

      /*        if(age < 0){
            //console.log('Age invalide');
            alert('age invalide');
        }else{
            let cat = age < 2 ? 
                "Bébé" : 
                age < 12 ? "Enfant" :
                age >= 12 && age < 25 ? 
                    "Adolescent" : "Adulte";
            //console.log(cat);
            alert(cat);
        }
*/
      //  console.log(confirm('Voulez-vous continuer ?'));

      /*      if (0){
        alert('Je suis true')
      } */

      // Les boucles

      //while
      //initialisation
      /*      let x = 0;

      while(x <= 10){ // Point d'arrêt
        // Instruction
        console.log(x)

        //Pas
        //x = x + 1;
        x++;
      }*/

      /*      let x = 10;

      while(x > 0){ // Point d'arrêt
        // Instruction
        console.log(x)

        //Pas
        //x = x + 1;
        x = x - 2;
      }
      */

      /*      let question = true

      while(question){
          let contin = confirm('Voulez-vous continuer ?')

          if(contin){
            contin = confirm('Voulez-vous continuer à jouer ?') 
          }else{
            question = false
            console.log('vous avez refusé')
          }
      }
      */

      //do while
      //---------

      /*      let x = 10;

      do{ // Point d'arrêt
        // Instruction
        console.log(x)

        //Pas
        //x = x + 1;
        x = x - 2;
      }while(x > 0); */

      // for
      //----

      /*      for(let x = 0; x <= 10; x++){
        coi, "x", multiplication, "=", i*multiplicationnsole.log(x)
      } */

      /*      for(let i = 1; i < 11; i++){
        const multiplication = 2

        console.log(`${i} x ${multiplication} = ${i*multiplication}`)
      } */

      /*      const notes = [1,2,3]

      let somme = 0

    for(let i = 0; i < notes.length; i++){
        somme = somme + notes[i]
      }

      console.log(somme)*/

      /*  const object = { a: 1, b: 2, c: 3 };

      let notes = [1,8,5,4,5]

      for(let i in notes){
          console.log(notes[i])
      }*/

      /*      const eleves = ["Jean", "Marc", "Marie"];
      for (const el of eleves) {
        console.log(el);    
        // affichera alternativement : Jean, Marc, Marie
      }

      for(let i = 1; i<6; i++){
          for(let j =1; j < 5; j++){
            console.log("* ");
          }
          console.log("\n");
      }
      */

      var n = 6;
      var str = "";
      for (let i = n / 2; i < n; i += 2) {
        // print first spaces
        for (let j = 1; j < n - i; j += 2) {
          str += " ";
        }
        // print first stars
        for (let j = 1; j < i + 1; j++) {
          str += "*";
        }
        // print second spaces
        for (let j = 1; j < n - i + 1; j++) {
          str += " ";
        }
        // print second stars
        for (let j = 1; j < i + 1; j++) {
          str += "*";
        }
        str += "\n";
      }
      // lower part
      // inverted pyramid
 /*     for (let i = n; i > 0; i--) {
        for (let j = 0; j < n - i; j++) {
          str += " ";
        }
        for (let j = 1; j < i * 2; j++) {
          str += "*";
        }
        str += "\n";
      }
      console.log(str);*/

/*      const a = [1, 10, 15, 25]

/*      for (let i = 0; i < a.length; i++) {
        console.log(a[i]);
      } 
      const obj = { a: 1, b: 2, c: 3 }

     for(el in obj){
        console.log(obj[el]);
      }

      for(karl in a){
        console.log(a[karl]);
      }*/

      //const sentence = '     Bonjour       ';
     /* const val = 5.01
      console.log(typeof val);
      console.log(typeof val.toString());*/
    //  const notes = [1, 8, 5, 4, 5];

    //console.log(notes.every( el => el >0))

      console.log(Math.floor(Math.random() * 11))
    </script>
  </body>
</html>
